import 'package:flutter/material.dart';

class SoalQuizView extends StatelessWidget {
  const SoalQuizView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Ini adalah Quiz'),
      ),
    );
  }
}
